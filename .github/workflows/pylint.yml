name: Pylint

on: pull_request

jobs:
  pylint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Pylint
        run: pip install pylint
      - name: Run Pylint
        run: pylint *.py --exit-zero
